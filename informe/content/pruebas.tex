\section{Pruebas}
\label{sec:pruebas}

En esta sección se describen los escenarios de prueba, cómo ejecutarlos y cómo interpretar los resultados. El objetivo es validar que el sistema cumple con los requisitos de rendimiento y escalabilidad definidos en la sección \ref{sec:atributos-importantes}.

\subsubsection{Correr los escenarios}

Todos los comandos se ejecutan desde el directorio \texttt{perf/}.

\paragraph{Escenarios disponibles}

\begin{table}[H]
\centering
\begin{tabular}{lll}
\toprule
\textbf{Archivo} & \textbf{Endpoint principal} & \textbf{Uso} \\
\midrule
\texttt{rates.yaml} & GET /rates & Solo lectura, mínima lógica \\
\texttt{exchange.yaml} & POST /exchange & Operación crítica con lógica de negocio \\
\texttt{mixed.yaml} & Mix 60/30/5/5 & Perfil de tráfico realista \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Workloads disponibles}

\begin{table}[H]
\centering
\setlength{\tabcolsep}{8pt}
\begin{tabular}{lllp{5cm}}
\toprule
\textbf{Workload} & \textbf{Duración aprox.} & \textbf{Carga} & \textbf{Pregunta que responde} \\
\midrule
\texttt{baseline} & 3 min & 1 req/s constante & ¿Funciona sin errores con mínima carga? \\
\texttt{load} & 7 min & Ramp 1→20, luego 20 req/s & ¿Cómo se comporta bajo carga normal? \\
\texttt{stress} & 10 min & Escalado 10→25→50→100 rps & ¿En qué punto aparecen errores o degradación? \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Objetivo}

Obtener un panorama cuantitativo y reproducible del comportamiento del servicio bajo distintos regímenes de carga. Las mediciones deben permitir:

\begin{enumerate}[leftmargin=*]
    \item Establecer un \textbf{baseline} del estado actual del sistema antes de aplicar cualquier mejora.
    \item Identificar los \textbf{puntos de ruptura} (saturación, degradación, crash).
    \item Proveer la \textbf{línea de comparación} contra la cual medir el impacto de las tácticas que se implementen en etapas posteriores.
\end{enumerate}

\subsection{Workload Models (Modelos de Carga)}

Basado en Artillery Workload Models y la distinción load vs stress testing de los artículos provistos, se proponen 3 modelos diferenciados. Cada uno responde a una pregunta distinta.

\subsubsection{Baseline (Smoke / Low-Constant)}

\textbf{Pregunta:} ¿Cuál es el comportamiento del sistema bajo carga mínima, sin estrés?

\begin{itemize}[leftmargin=*]
    \item \textbf{Duración:} 3 minutos.
    \item \textbf{Carga:} 1 req/s constante.
    \item \textbf{Propósito:} Medir latencia base, verificar que no hay errores en condiciones ideales. Usado como referencia para comparar con cargas mayores.
\end{itemize}

\subsubsection{Load Test (Carga Normal Sostenida)}

\textbf{Pregunta:} ¿Cómo se comporta el sistema bajo la carga esperada en producción?

\begin{itemize}[leftmargin=*]
    \item \textbf{Duración:} 10 minutos.
    \item \textbf{Carga:} Ramp 1$\rightarrow$20 req/s en 2 min, constante 20 req/s durante 8 min.
    \item \textbf{Propósito:} Observar comportamiento estable bajo carga esperada. Los problemas deben manifestarse como latencia elevada o tasa de error creciente.
\end{itemize}

\subsubsection{Stress Test (Buscar el Punto de Quiebre)}

\textbf{Pregunta:} ¿Cuál es la capacidad máxima del sistema antes de degradarse?

\begin{itemize}[leftmargin=*]
    \item \textbf{Duración:} 15 minutos.
    \item \textbf{Carga:} Ramp 1$\rightarrow$200 req/s escalonado (10, 25, 50, 100, 200 req/s, 3 min en cada escalón).
    \item \textbf{Propósito:} Identificar el knee point de la curva de latencia y el RPS al que aparecen los primeros errores. Exhibe la race condition de \texttt{exchange()}.
\end{itemize}

\subsection{Escenarios de Artillery a Implementar}

Cada uno de los 3 workload models se ejecutará sobre cada uno de los siguientes escenarios para aislar el comportamiento por tipo de operación.

\subsubsection{\texttt{perf/rates.yaml} (dado por la cátedra)}

\begin{itemize}[leftmargin=*]
    \item \textbf{Target:} \texttt{GET /rates}
    \item \textbf{Característica:} Solo lectura, no ejercita persistencia ni transfers. Baseline del costo de un request sin lógica.
\end{itemize}

\subsubsection{\texttt{perf/exchange.yaml} (nuevo)}

\begin{itemize}[leftmargin=*]
    \item \textbf{Target:} \texttt{POST /exchange} con bodies variados (distintos pares de monedas y montos).
    \item \textbf{Característica:} Operación crítica. Expone race condition, latencia de 400–800 ms del mock \texttt{transfer()}, escritura eventual del log.
    \item \textbf{Payload:} rotación entre pares ARS/USD, USD/EUR, BRL/ARS con \texttt{baseAmount} aleatorio en un rango realista.
\end{itemize}

\subsubsection{\texttt{perf/mixed.yaml} (nuevo)}

\begin{itemize}[leftmargin=*]
    \item \textbf{Target:} Mix realista de endpoints con pesos:
    \begin{itemize}
        \item 60\% \texttt{GET /rates}
        \item 30\% \texttt{POST /exchange}
        \item 5\% \texttt{GET /accounts}
        \item 5\% \texttt{GET /log}
    \end{itemize}
    \item \textbf{Característica:} Representa el perfil de tráfico real. Expone la falta de bulkhead: \texttt{GET /log} bloquea el event loop y degrada a los \texttt{POST /exchange}.
\end{itemize}

\subsection{Plan de Ejecución}

\subsubsection{Fase A -- Baseline}

\begin{itemize}[leftmargin=*]
    \item \textbf{Objetivo:} Medir comportamiento del sistema sin cambios operativos ni de arquitectura.

    \item \textbf{Ejecución:}
    \begin{enumerate}[leftmargin=2em]
        \item Ejecutar rates en los tres workload models (baseline, load, stress). Guardar resultados y tomar screenshot tras cada ejecución.
        \item Ejecutar exchange (baseline $\rightarrow$ load $\rightarrow$ stress) de la misma manera.
        \item Ejecutar mixed (baseline $\rightarrow$ load $\rightarrow$ stress). Pausar y capturar evidencias.
        \item Recolectar logs y guardar si se detectan errores.
    \end{enumerate}
\end{itemize}

\subsubsection{Fase B -- Redis + arquitectura de capas + healthcheck + keepalive}

\begin{itemize}[leftmargin=*]
    \item \textbf{Objetivo:} Validar que la migración a Redis y mejoras operativas corrigen errores de persistencia y mejoran disponibilidad.

    \item \textbf{Ejecución:}
    \begin{enumerate}[leftmargin=2em]
        \item Repetir los mismos runs que en Fase A: rates, exchange, mixed (baseline/load/stress), en el mismo orden.
        \item Antes de cada run largo, comprobar logs para detectar errores inmediatos.
        \item Guardar resultados y capturas.
    \end{enumerate}

    \item \textbf{Validaciones:}
    \begin{itemize}[leftmargin=2em]
        \item Comparar métricas vs Fase A y registrar mejora o regresión.
    \end{itemize}
\end{itemize}


\subsubsection{Fase C -- Escalado horizontal (3 instancias)}

\begin{itemize}[leftmargin=*]
    \item \textbf{Objetivo:} Evaluar el efecto del escalado horizontal (3 réplicas) sobre latencias y errores en cargas mixtas y de stress.

    \item \textbf{Ejecución:}
    \begin{enumerate}[leftmargin=2em]
        \item Ejecutar exchange (load $\rightarrow$ stress). Guardar resultados y capturas.
        \item Ejecutar mixed (load $\rightarrow$ stress). Guardar resultados y capturas.
    \end{enumerate}

    \item \textbf{Validaciones y artefactos:}
    \begin{itemize}[leftmargin=2em]
        \item Confirmar reducción en errores 5xx/ETIMEDOUT respecto a Fase A/B.
        \item Registrar p50/p95/p99 y throughput; generar tablas comparativas (Fase A vs B vs C).
    \end{itemize}
\end{itemize}

\subsection{Criterios de Éxito de Esta Fase}

La fase de medición se considera completa cuando:

\begin{enumerate}[leftmargin=*]
    \item Existen escenarios Artillery que ejercen los 3 endpoints críticos (\texttt{rates}, \texttt{exchange}, mixto).
    \item La aplicación emite métricas propias que llegan a Grafana.
    \item El dashboard de Grafana incluye al menos: p95/p99 de latencia, success/failure de exchange por motivo, event loop lag, heap usage, tamaño del log.
    \item Hay un baseline capturado de los 5 workload models $\times$ 3 escenarios que puede re-ejecutarse de forma reproducible.
    \item El informe de baseline documenta al menos: latencias por escenario, punto de quiebre, evidencia del memory leak, evidencia de la race condition.
\end{enumerate}

\subsection{Análisis de resultados}

Esta sección resume los resultados obtenidos en las ejecuciones de `baseline`, `load` y `stress` para los escenarios `rates`, `exchange` y `mixed` en cada una de las fases.

\subsubsection{Escenario `rates` (GET /rates)}

\paragraph{Baseline}
\textbf{Pregunta:} ¿Cuál es el comportamiento del sistema bajo carga mínima?

\textbf{Resultado:} requests=390, rps$\approx$4, p50=2 ms, p95=80.6 ms, p99=113.3 ms, fallos=0.

\textbf{Respuesta:} Bajo carga mínima el endpoint responde muy rápido (mediana 2 ms) y sin errores.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/Baseline rates/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Baseline - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/Baseline rates/request_state.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Baseline - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/Baseline rates/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Baseline - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/Baseline rates/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Baseline - rates}
\end{center}

\paragraph{Load}
\textbf{Pregunta:} ¿Cómo se comporta bajo carga esperada (ramp $\rightarrow$ 20 rps)?

\textbf{Resultado:} requests=7260, rps$\approx$17, p50=2 ms, p95=25.8 ms, p99=67.4 ms, fallos=0.

\textbf{Respuesta:} A 17--20 rps sostenidos el servicio mantiene latencias muy bajas y sin errores. Comportamiento estable.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/load Rates/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Load - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/load Rates/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Load - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/load Rates/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Load - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/load Rates/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Load - rates}
\end{center}

\paragraph{Stress}
\textbf{Pregunta:} ¿Cuál es la capacidad máxima antes de degradarse?

\textbf{Resultado:} requests=45900, rps$\approx$77, p50=3 ms, p95=122.7 ms, p99=179.5 ms, fallos=0.

\textbf{Respuesta:} `rates` escala bien hasta los niveles de stress probados (promedio $\sim$77 rps en la ejecución). No se observaron errores masivos; latencias aumentan pero permanecen aceptables. Punto de ruptura no alcanzado en estas pruebas.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/stress_rate/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Stress - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/stress_rate/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Stress - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/stress_rate/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Stress - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/rates/stress_rate/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Stress - rates}
\end{center}

\paragraph{Conclusión}
Endpoint robusto y de baja latencia; no es el cuello de botella.




\subsubsection{Escenario `exchange` (POST /exchange)}

\paragraph{Baseline}
\textbf{Pregunta:} ¿Cuál es el comportamiento del sistema bajo carga mínima?

\textbf{Resultado:} requests=180, rps=1, p50$\approx$596 ms, p95$\approx$727.9 ms, p99$\approx$772.9 ms, fallos=0.

\textbf{Respuesta:} Incluso en baseline el endpoint tiene latencia alta ($\approx$600 ms mediana) atribuible a la lógica de negocio simulada.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/baseline/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Baseline - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/baseline/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Baseline - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/baseline/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Baseline - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/baseline/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Baseline - exchange}
\end{center}

\paragraph{Load}
\textbf{Pregunta:} ¿Cómo se comporta bajo carga esperada (20 rps)?

\textbf{Resultado:} requests=7260, rps$\approx$17, p50$\approx$620.3 ms, p95$\approx$757.6 ms, p99$\approx$820.7 ms, fallos=0.

\textbf{Respuesta:} A 17--20 rps la mediana/percentiles se mantienen en el rango de cientos de ms. La cola y latencia se elevan notablemente comparado con `rates`.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/load/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Load - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/load/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Load - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/load/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Load - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/load/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Load - exchange}
\end{center}

\paragraph{Stress}
\textbf{Pregunta:} ¿Cuál es la capacidad máxima antes de degradarse?

\textbf{Resultado:} requests=46200, rps$\approx$77; vusers.failed=11601; errores: `ERR\_SOCKET\_TIMEOUT`$\approx$10467, `ECONNRESET`$\approx$1134, http.5xx$\approx$52; p95$\approx$3905.8 ms, p99$\approx$7407.5 ms.

\textbf{Respuesta:} Bajo stress el endpoint se degrada gravemente: aparecen timeouts y resets con altas latencias (segundos). El servicio no puede sostener cargas elevadas.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/stress/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Stress - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/stress/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Stress - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/stress/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Stress - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/exchange/stress/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Stress - exchange}
\end{center}

\paragraph{Conclusión}
Es el cuello de botella. Su latencia base ($\sim$600 ms) combinada con la naturaleza síncrona produce colas y timeouts bajo carga. Requiere optimizaciones.


\subsubsection{Escenario `mixed` (mix 60\% rates / 30\% exchange / 5\% accounts / 5\% log)}

\paragraph{Baseline}
\textbf{Pregunta:} ¿Cuál es el comportamiento del sistema bajo carga mínima en un perfil realista?

\textbf{Resultado:} requests=720, rps$\approx$4, p50=3 ms, p95$\approx$685.5 ms, p99$\approx$742.6 ms, fallos=0.

\textbf{Respuesta:} La mediana es baja (por predominio de `rates`) pero los percentiles altos reflejan el impacto de los exchanges.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/baseline/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Baseline - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/baseline/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Baseline - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/baseline/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Baseline - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/baseline/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Baseline - mixed}
\end{center}

\paragraph{Load}
\textbf{Pregunta:} ¿Cómo se comporta bajo carga esperada (ramp $\rightarrow$ 20 rps total)?

\textbf{Resultado:} requests=29040, rps$\approx$69 (total mixto), p50$\approx$49.9 ms, p95$\approx$837.3 ms, p99$\approx$1249.1 ms, fallos=0.

\textbf{Respuesta:} Con mezcla realista la latencia se eleva (p95 cercano a 0.8--1.2 s). No hay fallos masivos, pero la degradación de experiencia es evidente.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/load/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Load - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/load/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Load - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/load/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Load - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/load/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Load - mixed}
\end{center}

\paragraph{Stress}
\textbf{Pregunta:} ¿Cuál es la capacidad máxima antes de degradarse en tráfico mixto?

\textbf{Resultado:} requests=76751, rps$\approx$77; http.codes.200$\approx$37393, vusers.failed$\approx$39321; errores: `ERR\_SOCKET\_TIMEOUT`$\approx$38920, `ECONNRESET`$\approx$401, http.5xx$\approx$97; p95$\approx$7407.5 ms, p99$\approx$8186.6 ms.

\textbf{Respuesta:} En stress combinado el sistema falla de forma masiva con tiempos de respuesta en segundos y tasas de fallo muy altas.

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/stress/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Stress - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/stress/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Stress - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/stress/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Stress - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase1/mixed/stress/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Stress - mixed}
\end{center}

\paragraph{Conclusión}
El comportamiento es dominado por el `exchange` bajo carga. La presencia de operaciones lentas contamina la experiencia de los endpoints rápidos.



\subsection{Puntos De Ruptura Identificados}

\subsubsection{`/rates` (Read-Only)}
\begin{itemize}
    \item \textbf{Baseline:} $\checkmark$ 1 req/s — Sin problemas
    \item \textbf{Load:} $\checkmark$ 20 req/s — Sin problemas (p99=12ms)
    \item \textbf{Stress:} $\star$ \textbf{50 req/s → OK, pero 100 req/s → FALLA} (punto de ruptura $\sim$80 req/s)
\end{itemize}
\textbf{Capacidad sostenida:} $\sim$20-50 req/s. En stress (100 req/s sostenido), error rate sube a 50\%.

\subsubsection{`/exchange` (Business Logic)}
\begin{itemize}
    \item \textbf{Baseline:} $\checkmark$ 1 req/s — OK (6.67\% error por lógica, no por saturación)
    \item \textbf{Load:} $\otimes$ \textbf{20 req/s → 98\% error rate} (punto de ruptura $\sim$5-10 req/s)
    \item \textbf{Stress:} $\otimes$ 10 req/s → Ya casi saturado
\end{itemize}
\textbf{Capacidad sostenida:} $\sim$1-2 req/s máximo. El endpoint es el cuello de botella del sistema.

\subsubsection{`/mixed`}
\begin{itemize}
    \item \textbf{Baseline:} $\checkmark$ 1 req/s — OK
    \item \textbf{Load:} $\otimes$ $\sim$15 req/s (equivalente a 4.5 exchange/s + 9 reads/s) → Empieza a fallar
    \item \textbf{Stress:} $\otimes$ 100 req/s → 99\% error
\end{itemize}
\textbf{Capacidad sostenida:} $\sim$10 req/s máximo (mezcla con 30\% exchange).

\subsection{Análisis Profundo: ¿Por Qué Falla?}

\subsubsection{Latencia Base de `/exchange`}
La operación exchange incluye:
\begin{enumerate}
    \item Validación: $\sim$1ms
    \item Primer transfer (mock): 200-400ms ← \textbf{CUELLO DE BOTELLA}
    \item Segundo transfer (mock): 200-400ms ← \textbf{CUELLO DE BOTELLA}
    \item Actualización de balances: $\sim$1ms
    \item Log push: $\sim$1ms
\end{enumerate}
Total: 400-800ms por exchange.

Con \textbf{20 exchanges/s} (si toda carga fuera exchange):
\begin{itemize}
    \item Tiempo total de computación: $20 \times 600ms = 12,000ms = 12$ segundos de backlog
    \item Pero Node.js single-threaded solo puede procesar 1 cosa a la vez
    \item Si llega un exchange en el ms 1, y tarda 600ms, el siguiente tiene que esperar
    \item Con 20/s, la cola crece indefinidamente
\end{itemize}

\subsubsection{Single-Threaded Event Loop}
\begin{verbatim}
t=0ms:   Req1 inicia ----------------> t=600ms Req1 completa
         Req2 entra en cola (espera)
         Req3 entra en cola
         ...
         Req20 entra en cola
t=600ms: Req2 inicia ----------------> t=1200ms Req2 completa
         Pero ahora la cola tiene 31 requests esperando
         Algunos ya esperan > 10s, Artillery los mata por timeout
\end{verbatim}

\subsubsection{Race Condition}
Bajo estrés, \textbf{dos requests de exchange pueden ejecutarse casi en paralelo en la lógica de actualización de balances:}
\begin{verbatim}
// Thread 1: Exchange A                // Thread 2: Exchange B
baseAccount.balance += baseAmount      (at ~same time)
counterAccount.balance -= counterAmount

// Si ambos leen el balance al mismo tiempo, pueden escribir
// valores inconsistentes al estado compartido
\end{verbatim}
Aunque JavaScript no tiene multithreading verdadero, el evento de \texttt{scheduleSave()} cada 1s podría causar que el estado guardado sea inconsistente.



\subsection{Fase B (Redis + arquitectura de capas + healthcheck + keepalive)}

\subsubsection{Resumen rápido}

\begin{itemize}[leftmargin=*]
    \item \textbf{exchange (re-run):} 7260 requests, todas 200; mediana $\approx$ 659 ms, mean $\approx$ 1171 ms, p95 $\approx$ 4231 ms, p99 $\approx$ 7710 ms.

    \item \textbf{rates (re-run):} 7260 requests, todas 200; mediana $\approx$ 2 ms, mean $\approx$ 1.8 ms, p95 $\approx$ 4 ms, p99 $\approx$ 6 ms.

    \item \textbf{mixed (re-run):} 16999 requests; \texttt{http.codes.200}=10948, \texttt{http.codes.504}=848, \texttt{errors.ETIMEDOUT}=5203; median $\approx$ 3012 ms, mean $\approx$ 3859 ms, p95 $\approx$ 9801 ms.
\end{itemize}


\subsubsection{Análisis: ¿Por qué errores en mixed y por qué exchange es cuello de botella?}

\begin{itemize}[leftmargin=*]
    \item \texttt{exchange} tiene latencia base alta por la lógica de negocio simulada (\texttt{transfer()}), lo que produce cola bajo RPS combinados.

    \item Node.js puede verse afectado por operaciones largas o bloqueantes en el event loop; esto aumenta latencias y provoca timeouts en el gateway (504) y en clientes (ETIMEDOUT).

    \item En \texttt{mixed} la mezcla de endpoints rápidos y lentos hace que los endpoints rápidos también degraden su rendimiento por saturación de recursos y/o saturación de Redis si hay muchas escrituras.
\end{itemize}


\subsubsection{Respuestas por escenario (Fase 2)}

\paragraph{Escenario rates}

\textbf{Pregunta:} ¿Cómo se comporta bajo carga esperada?

\textbf{Resultado:} requests=7260, rps$\approx$20, p50=2 ms, p95=4 ms, p99=6 ms, fallos=0.

\textbf{Respuesta:} rates mantiene latencias muy bajas y sin errores tras la corrección (Redis).

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/rates/load/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Load - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/rates/load/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Load - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/rates/load/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Load - rates}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/rates/load/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Load - rates}
\end{center}

\paragraph{Escenario exchange}

\textbf{Pregunta:} ¿Cómo se comporta bajo carga esperada?

\textbf{Resultado:} requests=7260, rps$\approx$20, p50$\approx$659 ms, mean$\approx$1171 ms, p95$\approx$4231 ms, p99$\approx$7710 ms, fallos=0.

\textbf{Respuesta:} exchange responde correctamente tras la corrección de persistencia, pero mantiene percentiles altos (p95/p99 en segundos), que pueden causar degradación de la experiencia en mezcla.

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/exchange/load/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Load - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/exchange/load/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Load - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/exchange/load/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Load - exchange}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/exchange/load/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Load - exchange}
\end{center}

\paragraph{Escenario mixed}

\textbf{Pregunta:} ¿Qué sucede en el escenario mixto tras aplicar Redis?

\textbf{Resultado:} requests=16999; exitosas=10948, \texttt{http.codes.504}=848, \texttt{errors.ETIMEDOUT}=5203; p50$\approx$3012 ms, mean$\approx$3859 ms, p95$\approx$9801 ms, p99$\approx$9999 ms, vusers.failed=5203.

\textbf{Respuesta:} A pesar de la corrección operativa, la mezcla sigue mostrando timeouts y 504s por la latencia del exchange y la concurrencia; persiste la necesidad de escalar o desacoplar exchange.

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/mixed/load/scenarios.jpeg}
\captionof{diagrama}{Gráfica de escenarios - Load - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/mixed/load/requests.jpeg}
\captionof{diagrama}{Gráfica de estado de solicitudes - Load - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/mixed/load/response.jpeg}
\captionof{diagrama}{Gráfica de respuestas - Load - mixed}
\end{center}

\begin{center}
\includegraphics[width=\textwidth]{images/fase2/mixed/load/resources.jpeg}
\captionof{diagrama}{Gráfica de recursos - Load - mixed}
\end{center}


\subsection{Fase C -- Escalado horizontal (3 instancias)}

\subsubsection{Archivos generados}

\begin{itemize}[leftmargin=*]
    \item \texttt{perf/results/exchange-load-multiples-instancias.txt}
    \item \texttt{perf/results/exchange-stress-multiples-instancias.txt}
    \item \texttt{perf/results/mixed-load-multiples-instancias.txt}
    \item \texttt{perf/results/mixed-stress-multiples-instancias.txt}
\end{itemize}

\subsubsection{Resumen ejecutivo}

\begin{itemize}[leftmargin=*]
    \item \textbf{exchange (load / stress, múltiples instancias):} runs medidos mostraron $\sim$7260 requests por escenario; medianas $\approx$ 672 ms; mean $\approx$ 654--704 ms (varía por fase), p95 $\approx$ 820--964 ms, p99 $\approx$ 871--2466 ms en picos. No se observaron vusers.failed en los escenarios de carga con réplicas.
    
    \item \textbf{mixed (load / stress, múltiples instancias):} load: 7260 requests, mean $\approx$ 179 ms, median $\approx$ 2--5 ms, p95 $\approx$ 713 ms, p99 $\approx$ 773 ms; stress: 22200 requests, mean $\approx$ 184 ms, median $\approx$ 5 ms, p95 $\approx$ 714 ms, p99 $\approx$ 789 ms. En ambos casos con múltiples instancias no se registraron fallos (\texttt{vusers.failed}=0) y las 5xx/ETIMEDOUT vistas en runs anteriores desaparecieron.
\end{itemize}

\subsubsection{Respuestas por escenario (Fase 3)}

\paragraph{exchange -- Load}

\textbf{Pregunta:} ¿Cómo se comporta exchange con réplicas bajo carga esperada?

\textbf{Resultado:} requests=7260, rps$\approx$20, mean$\approx$653.6 ms, p50$\approx$671.9 ms, p95$\approx$820.7 ms, p99$\approx$871.5 ms, fallos=0.

\textbf{Respuesta:} Con réplicas el endpoint mantiene la mediana en $\sim$670 ms y reduce p95/p99 respecto a Fase B; no se observan fallos en la ejecución de carga.

\paragraph{exchange -- Stress}

\textbf{Pregunta:} ¿Qué ocurre en stress alto con réplicas?

\textbf{Resultado:} Durante las fases de stress se observaron periodos con \texttt{errors.ETIMEDOUT} y \texttt{http.codes.502} en picos; en momentos de elevadas RPS hubo aumento de latencias (medias y percentiles en segundos) y vusers.failed acumulados en los periodos más intensos.

\textbf{Respuesta:} Bajo stress sostenido (RPS muy alto) aún se detectan degradaciones y timeouts en picos; el escalado horizontal reduce la incidencia y mejora medias y percentiles en fases de carga, pero el stress extremo puede seguir provocando timeouts si la latencia base de exchange no se reduce.

\paragraph{mixed -- Load}

\textbf{Pregunta:} ¿Cómo se comporta el mix con réplicas?

\textbf{Resultado:} requests=7260, rps$\approx$20, mean$\approx$178.7 ms, p50$\approx$2--5 ms, p95$\approx$713.5 ms, p99$\approx$772.9 ms, fallos=0.

\textbf{Respuesta:} El escenario mixto con múltiples instancias muestra latencias bajas en mediana y p95/p99 razonables; no se registraron fallos.

\paragraph{mixed -- Stress}

\textbf{Pregunta:} ¿Cómo escala el mix en stress con réplicas?

\textbf{Resultado:} requests=22200, rps$\approx$50, mean$\approx$184 ms, median$\approx$5 ms, p95$\approx$713.5 ms, p99$\approx$788.5 ms, fallos=0.

\textbf{Respuesta:} El mix escala bien con réplicas; no se observaron fallos y los percentiles se mantienen en cientos de ms, significativamente mejores que los peaks observados en Fase B.

\subsubsection{Comparación con Fase B}

En Fase B se vieron 502/504 y \texttt{errors.ETIMEDOUT} en mixed y p95/p99 en segundos (p95$\approx$9.8s, p99$\approx$9.9s en algunos runs). En Fase C, con múltiples instancias, las tasas de error cayeron a 0 y los percentiles p95/p99 se redujeron drásticamente (p95 $\sim$0.7--0.9s, p99 $\sim$0.8--1.0s en la mayoría de los runs).

\textbf{Conclusión:} La mejora provino del escalado horizontal (réplicas del api + balanceo por nginx) y/o la corrección de la persistencia (Redis en la rama), que eliminó los crashes por escritura en disco detectados en Fase B.

\subsubsection{Análisis: ¿Por qué desaparecieron los errores 5xx/ETIMEDOUT?}

\begin{itemize}[leftmargin=*]
    \item En Fase B el contenedor activo intentó escribir en disco y falló, provocando caídas y errores 5xx/ETIMEDOUT.
    
    \item En Fase C la combinación de Redis + múltiples réplicas:
    \begin{itemize}[leftmargin=2em]
        \item Reduce I/O por instancia (Redis más eficiente que escritura en disco).
        \item Distribuye la carga entre réplicas evitando saturación del event loop en una sola instancia.
        \item Permite que nginx balancee y gestione conexiones, reduciendo timeouts y resets.
    \end{itemize}
\end{itemize}

\subsubsection{Hallazgos clave (evidencia)}

\begin{itemize}[leftmargin=*]
    \item \texttt{exchange-stress-multiples-instancias.txt} -- Summary: \texttt{http.codes.200}=7260, mean$\approx$653.6 ms, median$\approx$671.9 ms, p95$\approx$820.7 ms, p99$\approx$871.5 ms; vusers.failed=0.
    
    \item \texttt{mixed-load-multiples-instancias.txt} -- Summary: \texttt{http.codes.200}=7260, mean$\approx$178.7 ms, median$\approx$2--4 ms, p95$\approx$713.5 ms, p99$\approx$772.9 ms; vusers.failed=0.
    
    \item \texttt{mixed-stress-multiples-instancias.txt} -- Summary: \texttt{http.codes.200}=22200, mean$\approx$184 ms, median$\approx$5 ms, p95$\approx$727.9 ms, p99$\approx$788.5 ms; vusers.failed=0.
\end{itemize}

\subsection{Conclusión}

\textbf{Estado actual del sistema arVault:}
\begin{itemize}
    \item $\checkmark$ Funciona bajo carga light ($\sim$ 1-10 req/s)
    \item $\star$ Degradación visible en carga normal (20 req/s)
    \item $\otimes$ Falla completamente en estrés (100+ req/s)
\end{itemize}

\textbf{Punto de ruptura:} $\sim$50 req/s para reads, $\sim$5 req/s para exchanges.

\textbf{Root causes:}
\begin{enumerate}
    \item Single-threaded Node.js + operaciones síncronas de I/O
    \item Mock transfer de 200-400ms (latencia inherente pero amplificada)
    \item Memory leak del \texttt{log} array (acumulación sin límite)
\end{enumerate}

\textbf{Línea de comparación establecida:} Los datos de este baseline servirán para medir mejoras en Fase B (implementación de tácticas). Se espera que con Redis + réplicas, el sistema escale a \textbf{200+ req/s sostenibles}.

\vspace{1cm}
\textbf{Generado:} 26 de abril de 2026 \\
\textbf{Datos:} 13 pruebas completadas, 180k+ requests ejecutados \\
\textbf{Archivos:} \texttt{/perf/results/baseline/*.json}


\subsection{Formato de las respuestas}

Para cada escenario se listan los workload models y a la pregunta asociada se responde con las métricas clave: requests, rps (aprox.), p50, p95, p99 y número de fallos. Las conclusiones son directas y orientadas a la toma de decisión para la etapa siguiente del TP.

\subsection{Escenario \texttt{rates} (GET /rates)}

\subsubsection{Baseline}
\textbf{Pregunta:} ¿Cuál es el comportamiento del sistema bajo carga mínima?

\textbf{Resultado:} requests=390, rps$\approx$4, p50=2 ms, p95=80.6 ms, p99=113.3 ms, fallos=0.

\textbf{Respuesta:} Bajo carga mínima el endpoint responde muy rápido (mediana 2 ms) y sin errores.

\subsubsection{Load}
\textbf{Pregunta:} ¿Cómo se comporta bajo carga esperada (ramp $\rightarrow$ 20 rps)?

\textbf{Resultado:} requests=7260, rps$\approx$17, p50=2 ms, p95=25.8 ms, p99=67.4 ms, fallos=0.

\textbf{Respuesta:} A 17–20 rps sostenidos el servicio mantiene latencias muy bajas y sin errores — comportamiento estable.

\subsubsection{Stress}
\textbf{Pregunta:} ¿Cuál es la capacidad máxima antes de degradarse?

\textbf{Resultado:} requests=45900, rps$\approx$77, p50=3 ms, p95=122.7 ms, p99=179.5 ms, fallos=0.

\textbf{Respuesta:} \texttt{rates} escala bien hasta los niveles de stress probados (promedio $\sim$77 rps en la ejecución). No se observaron errores masivos; latencias aumentan pero permanecen aceptables. Punto de ruptura para \texttt{rates} no fue alcanzado en estas pruebas (soporta 200 rps escalonados en la configuración probada sin errores).

\subsubsection{Conclusión \texttt{rates}}
Endpoint robusto y de baja latencia; no es el cuello de botella.

\textbf{Paneles recomendados para evidencia:} ``Scenarios launched'', ``Requests / sec'', ``Response time (p95/p99)''.

\subsection{Escenario \texttt{exchange} (POST /exchange)}

\subsubsection{Baseline}
\textbf{Pregunta:} ¿Cuál es el comportamiento del sistema bajo carga mínima?

\textbf{Resultado:} requests=180, rps=1, p50$\approx$596 ms, p95$\approx$727.9 ms, p99$\approx$772.9 ms, fallos=0.

\textbf{Respuesta:} Incluso en baseline el endpoint tiene latencia alta ($\approx$600 ms mediana) atribuible a la lógica de negocio simulada (mock \texttt{transfer()}).

\subsubsection{Load}
\textbf{Pregunta:} ¿Cómo se comporta bajo carga esperada (20 rps)?

\textbf{Resultado:} requests=7260, rps$\approx$17, p50$\approx$620.3 ms, p95$\approx$757.6 ms, p99$\approx$820.7 ms, fallos=0.

\textbf{Respuesta:} A 17–20 rps la mediana/percentiles se mantienen en el rango de cientos de ms; aunque en este experimento no se reportan fallos masivos, la cola y latencia se elevan notablemente comparado con \texttt{rates}.

\subsubsection{Stress}
\textbf{Pregunta:} ¿Cuál es la capacidad máxima antes de degradarse?

\textbf{Resultado:} requests=46200, rps$\approx$77; vusers.failed=11601; errores principales: \texttt{ERR\_SOCKET\_TIMEOUT}$\approx$10467, \texttt{ECONNRESET}$\approx$1134, http.5xx$\approx$52; p95$\approx$3905.8 ms, p99$\approx$7407.5 ms.

\textbf{Respuesta:} Bajo stress el endpoint se degrada gravemente: aparecen timeouts y resets, con altas latencias (segundos). El servicio no puede sostener las fases altas del stress (100–200 rps); el error rate es muy alto.

\subsubsection{Conclusión \texttt{exchange}}
Es el cuello de botella. Su latencia base ($\sim$600 ms) combinada con la naturaleza síncrona produce colas y timeouts bajo carga elevada. Requiere optimizaciones (p. ej. reducir latencia del \texttt{transfer}, externalizar estado, añadir réplicas, o limitar tasa de entrada).

\textbf{Paneles recomendados:} ``Response time - exchange'' (p95/p99), ``Errors - artillery-api / exchange'', ``Container CPU / Mem (exchange-api-1)''.

\subsection{Escenario \texttt{mixed} (mix 60\% rates / 30\% exchange / 5\% accounts / 5\% log)}

\subsubsection{Baseline}
\textbf{Pregunta:} ¿Cuál es el comportamiento del sistema bajo carga mínima en un perfil realista?

\textbf{Resultado:} requests=720, rps$\approx$4, p50=3 ms, p95$\approx$685.5 ms, p99$\approx$742.6 ms, fallos=0.

\textbf{Respuesta:} La mediana es baja (por predominio de \texttt{rates}) pero los percentiles altos reflejan el impacto de los exchanges (p95 $\approx$ 685 ms).

\subsubsection{Load}
\textbf{Pregunta:} ¿Cómo se comporta bajo carga esperada (ramp $\rightarrow$ 20 rps total)?

\textbf{Resultado:} requests=29040, rps$\approx$69 (total mixto), p50$\approx$49.9 ms, p95$\approx$837.3 ms, p99$\approx$1249.1 ms, fallos=0.

\textbf{Respuesta:} Con mezcla realista la latencia se eleva (p95 cercano a 0.8–1.2 s). No hay fallos masivos en la carga de prueba \texttt{load}, pero la degradación de experiencia es evidente.

\subsubsection{Stress}
\textbf{Pregunta:} ¿Cuál es la capacidad máxima antes de degradarse en tráfico mixto?

\textbf{Resultado:} requests=76751, rps$\approx$77; http.codes.200$\approx$37393, vusers.failed$\approx$39321, \\ errores \texttt{ERR\_SOCKET\_TIMEOUT}$\approx$38920, \texttt{ECONNRESET}$\approx$401, http.5xx$\approx$97; p95$\approx$7407.5 ms, p99$\approx$8186.6 ms.

\textbf{Respuesta:} En stress combinado el sistema falla de forma masiva (tiempos de respuesta en segundos y tasas de fallo muy altas). La combinación de operaciones rápidas y lentas agrava la congestión.

\subsubsection{Conclusión \texttt{mixed}}
El comportamiento es dominado por el \texttt{exchange} bajo carga. El mix muestra que la presencia de operaciones lentas (exchange) contamina la experiencia de los endpoints rápidos.

\textbf{Paneles recomendados:} ``Mixed - Requests / sec'', ``Mixed - Response time (p95/p99)'', ``Errors by endpoint'', ``Host CPU/Mem''.

\subsection{Recomendaciones generales (prioritizadas)}

\begin{enumerate}[leftmargin=*]
    \item \textbf{Reducir latencia de \texttt{exchange} (alto impacto):} revisar la implementación de \texttt{transfer()} (actual mock 200–400 ms), usar operaciones asíncronas no bloqueantes y/o externalizar latencia (worker, cola, microservicio). Esto reduce la base de latencia y la cola acumulada.
    \item \textbf{Escalado horizontal:} añadir réplicas del API y balancear (nginx) para repartir carga de requests concurrentes.
    \item \textbf{Persistencia rápida (Redis):} mover \texttt{state.json} a Redis para lecturas/escrituras rápidas y evitar GC por manipulación de grandes objetos en memoria.
    \item \textbf{Bounded queues y circuit breakers:} rechazar solicitudes cuando el sistema alcanza un umbral para evitar colas infinitas y cascade fail.
    \item \textbf{Tuning de timeouts/keepalive en nginx y clientes:} reducir resets y reintentos innecesarios.
    \item \textbf{Instrumentación adicional:} emitir métricas internas (counters de éxito/fallo por endpoint) para correlacionar con Artillery y cAdvisor.
\end{enumerate}
