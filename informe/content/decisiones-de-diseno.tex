\section{Decisiones de Diseño (C\&C)}
\label{sec:decisiones-de-diseño}

\subsection{Evolución: Del Monolito Acoplado a Arquitectura en Capas}

Originalmente, el servicio constaba de un monolito de una sola capa. Había tres archivos con roles distintos (\texttt{app.js} operando de HTTP handler, \texttt{exchange.js} manejando lógica de negocio, y \texttt{state.js} interactuando con la persistencia en disco) que se encontraban acoplados directamente sin una clara inyección de dependencias ni definición de interfaces seguras. Todo corría en el hilo de un único proceso de Node.js, penalizando \textit{Testability}, \textit{Maintainability} y \textit{Modifiability}. El caso base se ilustra debajo:
\begin{center}
\begin{tcolorbox}[
  breakable,
  colback=white,
  colframe=black,
  boxrule=0.5pt
]
\begin{verbatim}
            +-----------------------------------------------+
            |                 Cliente HTTP                  |
            |                    :5555                      |
            +-----------------------------------------------+
                                  |
                                  | HTTP
                                  v
            +-----------------------------------------------+
            |                    Nginx                      |
            |                :80  -> api:3000               |
            |             static/ -> store/static           |
            +-----------------------------------------------+
                                  |
                                  v
            +-----------------------------------------------+
            |            app.js (Express) :3000             | <- Presentación
            |-----------------------------------------------|
            | GET  /accounts   PUT  /rates                  |
            | GET  /rates      POST /exchange               |
            | GET  /log        PUT  /accounts/:id/balance   |
            +-----------------------------------------------+
                                  |
                                  | importa
                                  v
            +-----------------------------------------------+
            |                 exchange.js                   | <- Dominio
            |-----------------------------------------------|
            | exchange()      transfer() (stub ~200-400 ms) |
            | getAccounts()   setRate()                     |
            | setBalance()    getLog()                      |
            | getRates()                                    |
            +-----------------------------------------------+
                                  |
                                  | importa
                                  v
            +-----------------------------------------------+
            |              state.js                         | <- Persistencia
            |-----------------------------------------------|
            | init()          cheduleSave()                 |
            | load()                                        |
            +-----------------------------------------------+
                                  |
                                  | fs (JSON)
                                  v
            +-----------------------------------------------+
            |             app/state/                        | <- Disco
            |-----------------------------------------------|
            | accounts.json    log.json                     |
            | rates.json                                    |
            +-----------------------------------------------+

            -------------------------------------------------
                    Observabilidad (docker-compose)
            -------------------------------------------------
           cAdvisor --> statsd --> Graphite/StatsD --> Grafana
\end{verbatim}
\end{tcolorbox}

\captionof{diagrama}{Arquitectura monolítica del sistema}
\label{diag:arquitectura_monolito}
\end{center}

En exchange.js:
accounts = stateAccounts();  // alias al mismo array en memoria
rates = stateRates();
log = stateLog();
state.js guarda esas mismas referencias y las persiste con scheduleSave. exchange.js muta counterAccount.balance, baseAccount.balance, log.push(...) directamente — sin pasar por ningún método de state.js. Es decir, la capa de dominio escribe en la capa de persistencia bypaseando su interfaz.

app.js también mezcla responsabilidades
app.js: hace validación de input, llama al servicio Y decide el formato de respuesta todo en el mismo handler. No hay capa de controlador separada.

Después:
\begin{center}
\begin{tcolorbox}[
  breakable,
  colback=white,
  colframe=black,
  boxrule=0.5pt
]
\begin{verbatim}
  +-------------------------------------------------------------------------------+
  |                              Cliente (HTTP)                                   |
  |                                 :3000                                         |
  +-------------------------------------------------------------------------------+
                                      |
                                      v
  +-------------------------------------------------------------------------------+
  |                                 app.js                                        |
  |                       Express - registra rutas - init()                       |
  +-------------------------------------------------------------------------------+
            |                |                  |                    |
            v                v                  v                    v
  +----------------+ +----------------+ +----------------+ +----------------------+
  | account        | | rate           | | log            | | exchange             |
  | Controller     | | Controller     | | Controller     | | Controller           |
  |----------------| |----------------| |----------------| |----------------------|
  | getAll         | | getAll         | | getAll         | | postExchange         |
  | setBalance     | | setRate        | |                | |                      |
  +----------------+ +----------------+ +----------------+ +----------------------+
          |                  |                  |                      |
          v                  v                  |                      v
  +----------------+ +----------------+         |          +----------------------+
  | account        | | rate           |         |          | exchange Service     |
  | Service        | | Service        |         |          |----------------------|
  |----------------| |----------------|         |          | performExch.         |
  | getAll         | | getAll         |         |          +----------------------+
  | setBalance     | | setRate        |         |                      |
  +----------------+ +----------------+         |                      v
          |                  |                  |          +----------------------+
          v                  v                  |          | transfer Service     |
  +----------------+ +----------------+         |          |----------------------|
  | account        | | rate           |         |          | transfer() (mock)    |
  | Repository     | | Repository     |         |          +----------------------+
  |----------------| |----------------|         |                      |
  | findAll        | | getAll         |         |                      v
  | findById       | | getRate        |         |          +----------------------+
  | findByCurr.    | | setRate        |         |          | account + rate + log |
  | updateBal.     | |                |         |          | Repositories         |
  +----------------+ +----------------+         |          +----------------------+
          |                   |                 |
          +-------------------+-------+---------+
                                      |
                                      v
                             +----------------------+
                             | log Repository       |
                             |----------------------|
                             | findAll              |
                             | add                  |
                             +----------------------+
                                        |
                                        v
                     +---------------------------------------+
                     |         core/stateManager.js          |
                     |---------------------------------------|
                     | getAccounts()   getRates()   getLog() |
                     | saveAccounts()  saveRates()  saveLog()|
                     | scheduleSave() (cada 1s / 5s / 1s)    |
                     +---------------------------------------+
                                       |
                                       | fs (JSON)
                                       v
                     +---------------------------------------+
                     |           Archivos de estado          |
                     |---------------------------------------|
                     | accounts.json                         |
                     | rates.json                            |
                     | log.json                              |
                     +---------------------------------------+
\end{verbatim}
\end{tcolorbox}
\captionof{diagrama}{Arquitectura monolítica luego de la mejora}
\label{diag:arquitectura_monolito_despues}
\end{center}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{Capa} & \textbf{Archivos} & \textbf{Responsabilidad} \\
\hline
Presentación & controllers/ + app.js & HTTP, validación de estructura, mapeo de errores a status codes \\
\hline
Dominio & services/ & Reglas de negocio, validaciones de dominio (balance >= 0, etc.) \\
\hline
Persistencia & repositories/ + core/stateManager.js & Acceso y mutación del estado, lectura/escritura a disco \\
\hline
Utils & utils/errors.js + utils/validators.js & Tipos de error compartidos, funciones de validación puras \\
\hline
\end{tabular}
\captionof{tabla}{Capas de la aplicación y sus responsabilidades}
\end{table}


\subsection{Variables en memoria}

Accounts, rates y log son variables JavaScript dentro del proceso. Leer es instantáneo (bueno para performance), pero hace imposible escalar horizontalmente porque cada instancia tendría su propio estado desincronizado. Además genera la race condition: dos requests concurrentes pueden leer el mismo balance, pasar ambos el check de fondos, y producir doble gasto.


\subsection{Persistencia lazy}

El estado se guarda a disco cada 1-5 segundos en lugar de en cada operación. Simplifica el código y no bloquea requests con I/O, pero si el proceso muere entre guardados se pierden transacciones, violación de durabilidad.

\subsection{Persistencia en archivos json}

Todo vive en archivos JSON locales. Sin transacciones atómicas, sin estado compartible entre instancias, y con riesgo de corrupción si el archivo falla.


\subsubsection{Función transfer() mockeada de forma secuencial}

\begin{lstlisting}[language=javascript, caption={Ejemplo de función \texttt{transfer()}}, label={lst:transfer}]
async function transfer(fromAccountId, toAccountId, amount) {
 const min = 200;
 const max = 400;
 return new Promise((resolve) =>
   setTimeout(() => resolve(true), Math.random() * (max - min + 1) + min));
}
\end{lstlisting}

La función ignora todos sus parámetros y siempre resuelve true tras un delay aleatorio de 200–400 ms.


\subsection{Instancia única sin redundancia ni healthcheck}

Impacto sobre Availability
La API es un Single Point of Failure (SPOF). Un crash del proceso Node.js (error no capturado, OOM, señal del OS), un reinicio por deploy o cualquier excepción no manejada detiene completamente el servicio. Sin `healthcheck` declarado, Docker y Nginx no pueden detectar automáticamente que el contenedor dejó de responder. Nginx continuará enrutando requests al upstream caído, retornando 502 Bad Gateway a todos los usuarios.

Impacto sobre User-Perceived Performance
Sin distribución de carga entre instancias, todos los requests concurrentes comparten el único event loop de Node.js. Las esperas de `await transfer()` (400–800 ms por request) bloquean efectivamente el procesamiento del resto de requests que llegan mientras tanto.

Impacto sobre Scalability
Nginx no está configurado para balancear hacia múltiples backends. Aunque se pudieran levantar réplicas (`docker compose up --scale api=3`), el upstream hardcodeado a `exchange-api-1` ignorará las instancias adicionales. Para habilitar el balanceo se requiere modificar la configuración de Nginx.

\subsection{Falta de logs de operaciones}
Impacta en el no repudio (security) y la falta de poder hacer una auditoría.