upstream api { 
    server exchange-api-1:3000;
    keepalive 32;
}

server {
    listen 80;

    location / {
        proxy_pass http://api/;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_read_timeout 10s;
    }
}